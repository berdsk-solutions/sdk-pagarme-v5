next-version: 0.1.0
mode: Mainline
branches:
  main:
    tag: ""
    increment: Patch
    regex: ^main$
    is-release-branch: true
  develop:
    tag: preview
    increment: Minor
    regex: ^develop$
    source-branches: [ 'main' ]
    is-release-branch: false
ignore:
  sha: []