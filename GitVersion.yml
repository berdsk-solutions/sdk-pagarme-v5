next-version: 0.1.0
mode: ContinuousDelivery
branches:
  main:
    regex: ^main$
    tag: ""
    increment: Patch
    is-release-branch: true
  develop:
    regex: ^develop$
    tag: preview
    increment: Minor
    is-release-branch: false
    source-branches: ['main']
ignore:
  sha: []